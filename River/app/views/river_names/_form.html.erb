<%= form_for(@river_name) do |f| %>
  <% if @river_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@river_name.errors.count, "error") %> prohibited this river_name from being saved:</h2>

      <ul>
      <% @river_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
