<!DOCTYPE html>
<html>
<head>
  <title>Rivers On Rails</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar">
    <div class="navbar-inner">
      <a class="brand" href="#">Michigan Rivers</a>
      <ul class="nav">
        <li class="active"><a href="http://localhost:3000/">Home</a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            Rivers
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <% River.all.each do |river| %>
              <%= link_to river.name, :controller => "river", :action => "index", :id => river.id %><br>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
  <div class="container-fluid">
    <%= yield %>
  </div>
</body>
</html>
